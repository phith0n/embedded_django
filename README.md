# A minimal django application

Usage:

```
git clone https://github.com/phith0n/embedded_django.git
cd embedded_django

vim app.py
# Modify DEBUG and SECRET_KEY, and put your templates in the "templates" folder.

# if debug mode = True, running server
python app.py runserver

# if debug mode = False, running server
python app.py
```